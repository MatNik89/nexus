# Phase 4 verification round 5 — Codex

Target: `slice/p0-phase4` at `f09fe31b5dd284aa0dd393000613dbcba5e67275`; scoped fold diff `ee93c46..f09fe31`.

Evidence:

- All scoped production files are tracked at HEAD.
- `CGO_ENABLED=0 go vet ./... && CGO_ENABLED=0 go test -count=1 ./...` — PASS (all packages).
- The five folds and their detectors were traced only through the last commit. Green tests are not treated as proof where the oracle omits a reachable counterexample.

1. [UNFOLDED] The full forged DONE chain is still reachable after the first legitimate task completion because the supposedly process-local capability is serialized into the canonical event. `donePayload.Cap` is JSON (`internal/obligation/obligation.go:129-142`), `MarkTaskDone` puts the live process capability into it (`:969-1003`), and `Journal.Replay` exposes every canonical payload to any in-process caller (`internal/kernel/journal/journal.go:608-650`). The capability is global for all tasks in that manager and is neither per-task nor single-use. Concrete chain: let task A finish legitimately; replay its `task_done` and read `cap`; append task B's valid create/intent/canonical-marker attestation; append B's DONE with the recovered cap and the public literal verifier string. `Events` accepts it and the projection reaches DONE although B's file artifact never existed and no checker ran (`internal/obligation/obligation.go:242-253,449-489`). `TestForgedDoneChainDiesAtCapability` checks only empty and guessed-wrong tokens and never obtains the real token from a prior event (`internal/obligation/obligation_test.go:567-595`). The declared no-signature/no-crypto P0 ceiling is acceptable; it does not require persisting a bearer secret. Pass an unexported pointer/closure capability out-of-band to journal admission, or provide a journal-owned restricted DONE emitter, and never place the authority token in canonical bytes.

2. [OK] The parallel RED is now causal. `RunTask` adds an atomic sequence to the operation ID, so the two calls do not collide at `Authority.Issue` (`internal/obligation/obligation.go:907-928`). The test holds call A inside the real handler after the in-flight lease is acquired, then enters call B synchronously and requires the specific “in progress” refusal before releasing A (`internal/obligation/obligation_test.go:483-517`). Removing `inflight.LoadOrStore` makes B reach the blocking handler (and attempt the already-closed barrier), so the detector turns red rather than being rescued by the grant layer.

3. [OK] Migration classification is now typed on every legacy shape named in round 4. Schedule and obligation export package sentinels and wrap them with `%w`; `errors.Is` survives journal projection/catch-up wrapping (`internal/schedule/schedule.go:212-217,249-251`; `internal/obligation/obligation.go:42-46,360-380,406-414,449-481`; `internal/kernel/journal/journal.go:357-372`). This covers v1 schedule creation, delivered, acked, task-executed, and task-done. The schedule RED now asserts `errors.Is` (`internal/schedule/schedule_test.go:451-457`). There is no dedicated old-delivered/old-acked fixture, which is the weakest proof point, but the direct projection branches unambiguously wrap the same sentinel and no alternate replay path exists.

4. [UNFOLDED] Health handling improved but is not fail-closed end-to-end. Doctor now maps an existing unreadable health path to OFF, and daemon write failures reach stderr (`internal/preflight/doctor/doctor.go:145-165`; `cmd/nexus/main.go:133-156`). However, ENOENT is always reported OK as “daemon not started,” even when the daemon is live and its health write just failed; doctor does not correlate the existing heartbeat (`doctor.go:156-158`). Also, `go b.sched.Run(...)` and the health goroutine start independently: the first `mirror()` can run before the startup sweep stores its result, writing a false healthy file; only the five-second poll may correct it (`cmd/nexus/main.go:122-155`). A process exit in that window loses the startup failure durably. The added doctor RED covers directory/unreadable, non-empty, malformed, and absent-in-isolation, but not “fresh daemon heartbeat + missing health mirror” or this startup ordering (`internal/preflight/doctor/doctor_test.go:129-169`). Initialize the health surface synchronously before declaring readiness, publish each sweep result rather than polling shared latest state, and treat missing health as OFF when daemon liveness exists.

5. [OK] The new SIGKILL detector reaches the intended crash point. In the test binary, the seam executes after the first `insertInTx` and before transaction commit (`internal/kernel/journal/journal.go:509-555`). The child first commits the schedule separately, then dies inside the multi-member fire recipe; reopen requires the schedule to remain while every recipe member is absent, and a recovery sweep must fire exactly once (`internal/schedule/schedule_test.go:588-662`). A sequential-append replacement either bypasses the seam and exits zero or leaves a partial recipe, so the RED is causal. Production activation is safely blocked by `testing.Testing()`: Go sets that value true only in `go test` binaries and false in ordinary `go build`, so the environment variable alone cannot trigger SIGKILL in the shipped binary. The test machinery is compiled as a dependency of the production package, but no production runtime activation path was found in the scoped diff.

6. [OK] No additional material regression was found in the last fold diff. Removing attacker-supplied `expected_path` from `task_executed` and deriving the notes path from the manager's immutable profile root closes the prior path-laundering route while keeping projection replay filesystem-free (`internal/obligation/obligation.go:122-127,416-442,969-982`). This does not compensate for the capability disclosure in finding 1.

Strongest counterargument: an arbitrary model response cannot call `Journal.Replay`; only compiled in-process code can. That is not sufficient here because the claimed guard explicitly targets a “generic in-process producer,” `Manager.Journal()`/`Journal.Replay` are exposed composition APIs, and the journal boundary is the locked owner of legal durable state. Once the bearer appears in that same journal, the boundary no longer distinguishes checker-owned admission from another producer in the stated threat model.

VERDICT: FAIL
