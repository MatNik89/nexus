# PHASE-0 code review round 5 — Codex

Scope: narrow convergence verification of code `bb5b4c611b24ff8c6a8d67adacd61713d4087214`, task amendment `68fcb2ee27da24aa2cbe1504e5130878f928de02`, and report `8358b5f372a655097a212c521967643c90a443cf`. Only the seven claimed folds and defects in lines changed by `bb5b4c6` were reviewed.

Fresh evidence:

- `CGO_ENABLED=0 go test -count=1 -v ./internal/preflight/... ./internal/buildcheck/...` — PASS: doctor 3 top-level tests, probe 31 tests (57.923 s), buildcheck 3 tests.
- `CGO_ENABLED=0 go vet ./...` — PASS.
- `scripts/static-check.sh <fresh probehelper>` — PASS; the shadowing test target is confirmed statically linked.
- `git diff --name-only 68fcb2e..8358b5f -- . ':(exclude)docs/PROBE-REPORT-2026-09-03.md'` — empty; the report is the only post-task change.
- A temporary diagnostic through the production launcher showed both shadowing writes fail with `ENOENT`, not a read-only-filesystem error.

1. **[NEW-ERROR] The hostile-XDG regression can recursively delete pre-existing user data.** `TestHostileXDGRuntimeDirCannotWidenRoots` uses the fixed path `$HOME/.nexus-probe-red`, calls `MkdirAll`, and unconditionally defers `os.RemoveAll` (`internal/preflight/probe/probe_linux_test.go:636-650`). If that directory already exists, the test accepts it and deletes its entire contents. It is also collision-prone across concurrent runs. This violates the constitution's fresh-fixture rule and is unacceptable in a security suite intended for the real deployment host. Use `os.MkdirTemp(home, ".nexus-probe-red-*")`, verify the returned path is newly test-owned, and clean up only that unique directory.

2. **[UNFOLDED] The library-shadowing REDs are vacuous and do not prove `--remount-ro /`.** `TestRootfsReadOnlyPreventsLibShadowing` launches the statically linked `probehelper` and accepts any write error (`internal/preflight/probe/probe_linux_test.go:619-632`). A fresh diagnostic produced `open /nexus-libs/libc.so.6: no such file or directory` and `open /lib/evil: no such file or directory`: this target has no interpreter or libraries, so neither parent directory exists. The test therefore remains green if `--remount-ro /` is deleted. Use a dynamic fixture whose closure creates `/nexus-libs` and `/lib`, require `EROFS`/permission denial rather than `ENOENT`, and add a test-only negative control that omits the remount and proves the same writes succeed. The production layout and remount (`internal/preflight/probe/probe.go:254-257,579-634`) are plausible, but the claimed RED evidence does not establish them.

3. **[UNFOLDED] The closure budgets are bounded but still not enforced before all budget-attributable allocations.** `loadBounded` correctly caps each individual file using one descriptor, but `addBuf` checks the 256 MiB aggregate only after the next file has already been read into memory (`internal/preflight/probe/probe.go:142-166,274-292,296-320,369-381`). A fifth 64 MiB member can therefore allocate before a full 256 MiB closure is rejected. Likewise, `parseELFMeta` materializes the complete `ImportedLibraries` slice, and `rootNodes`/`childNodes` are fully allocated before `push` checks the 256-node queue limit (`internal/preflight/probe/probe.go:180-210,333-347,374-393`). A crafted sub-64-MiB ELF with a very large repeated `DT_NEEDED` table can consume far more than the advertised queue budget before rejection. Pass the remaining aggregate byte allowance into `loadBounded`; reject `len(meta.needed)` and `len(queue)+len(meta.needed)` before constructing node slices, or parse DT_NEEDED under a bounded iterator. Add aggregate-overflow and over-cap dependency-table REDs.

4. **[OK] Transitive old-dtags RPATH inheritance is now implemented and exercised at the real loader boundary.** Queue nodes carry ancestor RPATH separately; a library's own RPATH extends that chain, while RUNPATH remains direct-only (`internal/preflight/probe/probe.go:169-210,239-251,324-395`). The cc fixture gives only the main executable an old-dtags RPATH covering both direct and transitive unique libraries, and the sandboxed executable completed successfully (`internal/preflight/probe/probe_linux_test.go:654-691`). An additional temporary mixed ancestor-RPATH/child-RUNPATH precedence fixture selected the same child library as the host loader.

5. **[OK] Content identity and the single-file size/growth cap are correctly folded.** `loadBounded` opens once, fstats that descriptor, and reads through a `maxClosureOneFile+1` limiter; parsing, hashing and memfd pinning all consume the returned bytes (`internal/preflight/probe/probe.go:142-210,259-320,369-381`). Path replacement after opening cannot change the parsed/pinned object, and file growth cannot make this read unbounded. Finding 3 is the distinct aggregate/metadata-allocation remainder.

6. **[OK] Ambient work-root widening is closed in production code.** Candidate roots are canonicalized and admitted only as either current-user-owned mode-0700 directories under `/run/` or root-owned sticky temp roots; arbitrary `TMPDIR`/`XDG_RUNTIME_DIR` values are ignored (`internal/preflight/probe/probe.go:399-439`). Candidate paths still must canonicalize beneath a root, be current-UID-owned directories, and reject `0o022` (`internal/preflight/probe/probe.go:442-480`). The hostile-home case passed, although its fixture cleanup must be repaired per finding 1.

7. **[OK] The report now states the implemented workdir rule exactly and is revision-bound.** It says owner-owned with no group/world write, under the two validated root classes (`docs/PROBE-REPORT-2026-09-03.md:17-21`), and its 31 listed probe tests match the current discovered test set. It identifies tested revision `68fcb2e`, and the only subsequent change through `8358b5f` is the report itself (`docs/PROBE-REPORT-2026-09-03.md:1-56`). The “lib shadowing impossible” evidence claim remains unsupported until finding 2's RED is made causal.

8. **[OK] The YOLO real-backend RED is now ordered after the real backend and effect-path integration.** T24 retains only policy/HITL and remote-cannot-enable checks (`docs/tasks-P0.md:273-287`); T25/T26 then supply and bind the real backend (`docs/tasks-P0.md:289-307`); T27 owns the unchanged hostile-suite-under-YOLO integration (`docs/tasks-P0.md:309-325`). This folds round-4 finding #5 without weakening the locked confirmations-only bypass.

9. **[OK] Relative RUNPATH components are rejected fail-closed.** Expansion keeps only absolute paths after supported `$ORIGIN` substitution (`internal/preflight/probe/probe.go:213-237`), and the unit regression includes `lib` and `../rel` as dropped components (`internal/preflight/probe/probe_linux_test.go:571-584`). No cwd-derived host search remains through this path.

10. **[OK] Libraries now have one non-workdir runtime namespace.** Resolved libraries are pinned beneath `/nexus-libs`, inherited environment is cleared, `LD_LIBRARY_PATH` names only that directory, and the root remount occurs after all mounts (`internal/preflight/probe/probe.go:254-257,374-381,579-634`). No new host-path injection was found in these changed lines. The containment implementation is distinct from the non-red-capable shadowing test in finding 2.

VERDICT: FAIL
