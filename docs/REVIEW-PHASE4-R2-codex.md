# Phase 4 verification round 2 — Codex

Target: `slice/p0-phase4` at `f1d388952d0aebb377735e17cd27dffef070c170`; fold diff `b298083..f1d3889`.

Evidence:

- `CGO_ENABLED=0 go vet ./... && CGO_ENABLED=0 go test -count=1 ./...` — PASS.
- `CGO_ENABLED=0 go test -count=50 ./internal/schedule ./internal/obligation` — PASS.
- `CGO_ENABLED=0 go test -count=1 ./cmd/nexus` — PASS.
- The green suite does not exercise several failure paths below; those are demonstrated directly by reachable code/state transitions.

1. [OK] Round-1 #1 is folded. `internal/schedule/schedule.go:388-404` builds the fire, run-admission, and decorator events before one `AppendBatch`; a decorator error appends nothing, and any projection failure rolls back the whole batch. `cmd/nexus/main.go:219` wires `Manager.FireParams`. The fire↔obligation-admission crash window is closed.

2. [UNFOLDED] Round-1 #2 (also kilo #2 / agy #2) remains materially unfixed. `internal/obligation/obligation.go:568-599` persists a timestamp, but `MarkDelivered` is still merely an unrestricted local state-transition method and `MarkAcked` still manufactures both checker records itself. The “user-gesture” evidence is `m.clock.Now()` created inside the method seeking PASS; neither record carries a channel receipt/event identity or authenticated acknowledgment source. The production RED itself calls `b2.obl.MarkDelivered(...)` directly (`cmd/nexus/main_test.go:268`). Thus any internal caller can still execute `MarkDelivered(occ); MarkAcked(occ)` with no delivered message and no independent user gesture. `delivered_at` is real journal time, but the B5 evidence remains ceremonial. Fix: admit transport-owned delivery-receipt and user-ack artifacts, bind both to the same occurrence/event identities, and grade those artifacts rather than locally synthesized structs.

3. [OK] The narrow defect in round-1 #3 is folded: `RunTask` now creates a sealed call/grant and dispatches through `EffectRunner` (`internal/obligation/obligation.go:661-689`); the production runner is a real `EffectPath` (`cmd/nexus/main.go:220-238`). New integrity defects in that path are findings 16-19 below.

4. [OK] Round-1 #4 is folded. `task_create`, `task_file_note`, and `task_done` are in the production specs, rules, and handler map (`internal/obligation/tools.go:27-48,101-152`) and therefore are planner-reachable.

5. [UNFOLDED] Round-1 #5 is only folded for one sequential crash shape. `executeGoverned` reads `intent_op` before appending intent (`internal/obligation/obligation.go:616-650`), while projection admission overwrites `intent_op` for every OPEN task with no empty-value CAS (`:314-326`). Two concurrent executions can both observe no intent, append different intents, and both call the handler; `fileNoteMu` serializes the two appends but does not deduplicate them. The existing RED (`obligation_test.go:321-379`) manually creates one post-effect/pre-attestation crash and retries sequentially, so it cannot catch this reachable duplicate-effect race. Fix: claim execution with a compare-and-set transition in the append transaction, bind executed/reconciled events to that operation ID, and make other claimants reconcile or refuse.

6. [OK] Round-1 #6 is folded within the declared ceiling. `FileContainsLine` grades the exact path+line, the handler serializes the shared read-modify-write, and later legitimate lines no longer invalidate an earlier task (`internal/obligation/obligation.go:380-427,726-765`; `internal/kernel/checker/checker.go:273-288`). The acknowledged verifier-read→DONE external-truncation gap is not relitigated here.

7. [OK] Round-1 #7 is folded for newly admitted schedules. `due_utc` is placed in the canonical created payload and projection and Sweep consumes the persisted value (`internal/schedule/schedule.go:119-126,173-211,334-370`). Upgrade compatibility is a separate new defect in finding 20.

8. [OK] Round-1 #8 and the matching kilo/agy Lord Howe finding are folded. The search selects the earliest matching instant across non-hour folds and the Lord Howe test pins the 30-minute case (`internal/schedule/schedule.go:84-100`; `schedule_test.go:301-325`).

9. [OK] Round-1 #9 is folded. Admission round-trips the civil date and rejects `2026-02-30` (`internal/schedule/schedule.go:58-74`; `schedule_test.go:328-342`).

10. [UNFOLDED] Round-1 #10 is internally inconsistent with the selected policy. `Run(ctx, interval, ...)` uses `interval` for its ticker but never updates `s.sweepInterval`; Sweep grades OVERDUE against the independently retained/default field (`internal/schedule/schedule.go:368-370,443-466`). Calling `Run(..., 5*time.Minute, ...)` therefore still marks a reminder 31 seconds late as OVERDUE although the comment and resolution say tolerance *is the mechanism's tick*. Existing boundary tests call `Sweep` directly with the 30-second default. Use one owned interval value for both ticking and classification.

11. [UNFOLDED] Round-1 #11 is only partially folded. Reconciliation now runs every sweep, but `mirrorCounter` discards both the authoritative query error and `AtomicWriter` error (`internal/schedule/schedule.go:411-420`). Doctor still maps every read failure—including EACCES/I/O—to `StatusOK` “counter file absent” and accepts arbitrary/malformed contents as OK (`internal/preflight/doctor/doctor.go:118-128`). A broken mirror therefore still reports healthy forever. Return/record mirror failures and make doctor distinguish ENOENT-before-first-fire from corruption or inaccessible state.

12. [OK] Round-1 #12 is folded. The journal DSN now uses `_txlock=immediate` (`internal/kernel/journal/journal.go:158`), which modernc v1.58 documents and maps to immediate transaction begin mode.

13. [UNFOLDED] Round-1 #13 is only discriminator-sealed, not kind-schema-sealed. `Events("file_note")` rejects unknown kinds, but its `EvCreated` validator checks only ID/kind (`internal/obligation/obligation.go:147-158`), and `CreateTask` likewise never validates `task_params` (`:531-545`). The production `task_create` can therefore return a successful receipt for `kind:file_note, params:"not-json"`; execution later fails permanently. The original finding explicitly required kind-specific body/params validation. Bind each registered kind to an admission validator and reject malformed `file_note` content before the creation commit.

14. [UNFOLDED] Round-1 #14 is partially folded: restart-between-fold and concurrent-sweep REDs now exist, but the ledger's named SIGKILL-between-recipe-members test still does not. `TestOccurrenceAndRunAdmissionAtomic` observes only a successful batch; `TestAppendBatchAllOrNothing` injects a pre-commit error in the generic primitive. Neither kills a process through `Scheduler.Sweep`, so the scheduler's use of the primitive and post-crash recovery are not protected by the specified causal detector.

15. [UNFOLDED] Round-1 #15 / agy #3 is only partially folded. `TestReminderSpineAcrossRestart` crosses REPL→planner→PEP→journal and restart, but it manually invokes `Scheduler.Sweep` and directly invokes `MarkDelivered`; it does not run the production scheduler goroutine or a delivery receipt producer (`cmd/nexus/main_test.go:234-241,260-271`). It also uses yolo, so no test proves an exact-intent ASK approval for `reminder_set` or `task_create`. The test is useful composition evidence, but it does not establish the production/evidence seams claimed by the finding.

16. [NEW-ERROR] `internal/obligation/obligation.go:692-704`, `cmd/nexus/main.go:203-212` — the fold introduced mutable package-global `notesDir`. Building a second profile manager overwrites the first manager's reconciliation root; a work-profile retry can then inspect and attest the private profile's `notes.txt` (or miss its own effect and duplicate it). This directly violates HARDQ B3 / `CLAUDE.md:53-56` (“no mutable global current-profile lookup after admission”). Store the profile-bound reconciliation path/marker function immutably on `Manager` or the registry entry.

17. [NEW-ERROR] `internal/obligation/obligation.go:384-408,706-715` — the marker encoding is ambiguous because task IDs are unrestricted. Task `(id="a", note="b] c")` and task `(id="a] b", note="c")` both produce `[a] b] c`; after the first executes, reconciliation can attest the second without performing its effect. A task ID containing a newline also creates a multi-line write that `fileContainsLine` can never match. Use a collision-resistant/length-prefixed marker (or validate a closed task-ID alphabet and delimiters) at creation.

18. [NEW-ERROR] `internal/obligation/obligation.go:314-369` — the new intent/attestation protocol is not enforced by the projection. `EvTaskExecuted` succeeds for any OPEN task even when `intent_op=''`, carries no operation ID, and overwrites an earlier attestation; `EvTaskDone` succeeds for any OPEN task without requiring an attested marker. Any in-process journal producer can therefore create durable EXECUTED/DONE state while bypassing the new reconciliation/checker sequence. Enforce `empty intent -> one claimed operation -> matching execution -> done only with attestation` inside the synchronous projection transaction.

19. [NEW-ERROR] `internal/obligation/tools.go:101-116`, `internal/obligation/obligation.go:486-545`, `internal/kernel/journal/journal.go:447-459` — task creation can acknowledge content different from the approved content. If `params` contains a configured known secret, the exact ASK covers the original bytes, then journal admission silently redacts the nested `task_params` string and commits the changed task; the tool still returns success. Execution writes the redacted text, not the exact approved note. Reuse the journal-owned `RedactorRewrites` rejection seam already used by memory, before issuing a task-creation receipt.

20. [NEW-ERROR] `internal/schedule/schedule.go:126,173-210`, `internal/obligation/obligation.go:121-127,201-217`, `internal/kernel/journal/journal.go:337-369` — both projection versions were bumped while their old canonical event shapes were changed without an upcaster. On opening a v1 DB, catch-up resets and replays without re-running current admission validators. A v1 `schedule.created` has no `due_utc`, so it rebuilds with zero and fires immediately as epoch-overdue; a v1 `task_executed` has `expected_sha256` but no `marker_line`, so the rebuilt task loses its usable completion evidence. Add explicit v1 replay migration/upcast or fail startup with a typed migration requirement; do not silently reinterpret durable events.

21. [NEW-ERROR] `cmd/nexus/main.go:123-128`, `internal/schedule/schedule.go:443-456` — production still installs the obsolete post-fire callback `MarkDue` even though the decorator has already moved the obligation to `DELIVERY_PENDING` in the atomic batch. Every successful fire therefore makes the callback attempt an illegal second DUE transition, and both that error and every Sweep/decorator error are silently discarded. Current state happens to remain correct, but the production path continuously hides a failed lifecycle call and a deterministic decorator failure would leave a reminder silently stuck. Remove the stale callback and expose scheduler failures through owned health/logging rather than dropping them.

The other reviewers' material round-1 findings were spot-checked: their Lord Howe issue is folded (finding 8); their synthetic-ack issue remains (finding 2); agy's production-spine request is only partially demonstrated (finding 15). The declared D-Bus wake-hook deferral and verifier-read→DONE ceiling were accepted as scoped and were not reopened.

VERDICT: FAIL
