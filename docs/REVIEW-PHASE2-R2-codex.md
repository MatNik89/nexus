# Phase 2 verification round 2 (Codex)

Target: `slice/p0-phase2` at claimed HEAD `a9f88745a4c53d694af176331bbf1c4efd22ef14`; fold diff `17f5603..a9f8874`. I verified the committed tree separately from the working directory because the two are materially different.

1. **[NEW-ERROR] HEAD does not contain the production `cmd/nexus` entry point used by the fold or by the test run.** `.gitignore:5` contains the unanchored pattern `nexus`, which ignores the entire `cmd/nexus/` directory. `git ls-tree -r HEAD` and `git ls-files cmd/nexus/main.go` return no entry, while `git status --ignored --short cmd/nexus/main.go` reports `!! cmd/nexus/`. Consequently the workspace gate compiles an ignored local file that is absent from commit `a9f8874`; a `git archive HEAD` cannot run `CGO_ENABLED=0 go build ./cmd/nexus` (`directory not found`), despite `Makefile:5-6` making that the production build. This is a blocking artifact-identity failure, not a cosmetic ignore rule.

2. **[UNFOLDED] Round-1 #1 is only partially folded: the same-grant duplicate oracle is causal, but the provider grant is not bound to the provider target and failed S7 reporting is ignored.** `internal/llm/provider/provider.go:153-176` consumes a grant at the real HTTP boundary, and `TestTransportConsumesGrantExactlyOnce` correctly proves one wire call and one attempt. However, `transport` never checks `g.TargetID` against `p.host`; the tests deliberately issue `"provider:test"` for arbitrary `httptest` hosts (`internal/llm/provider/provider_test.go:223-229`) and still pass. In addition, `internal/llm/planner/planner.go:96-119` ignores every `Authority.Report` error. A provider implementation can return success without consuming the grant; `Report` then rejects the AUTHORIZED→SUCCEEDED transition, but the planner discards that rejection and returns the model answer. The P0.2 duplicate-use sub-oracle is airtight; the full target/state ownership contract is not.

3. **[UNFOLDED] Round-1 #2 gained a new S7 state leak in the re-ask fold.** In `internal/llm/provider/structured.go:126-158`, if `ReAsk` consumes its grant and then returns an error (the ordinary network-failure case), `reaskRaw` remains empty and no outcome is reported, leaving the attempt permanently RUNNING. If a callback returns valid bytes without consuming its grant, `Extract` ignores the failed `Report` and returns the value anyway. `TestReaskCallbackCannotSelfRetry` proves only duplicate consumption; it does not cover either missing-consumption or consumed-then-error state. Require a transport-owned attempt result and make every `Report` failure fatal; report the consumed failure on all callback exits.

4. **[UNFOLDED] Round-1 #3 still lacks exact-call binding and S7-owned live deadline/cancel.** `internal/kernel/effectpath/effectpath.go:58-63` defines `ToolTarget` from only `ToolID/ToolCallID`. A grant for a call can therefore authorize a modified call with the same IDs but different arguments, schema hash, effect, deadline, idempotency key, or `ProfileID`; the RED changes only `ToolCallID`. Also, `internal/kernel/effectpath/effectpath.go:301-315` checks `time.Now()` in S6 rather than S7, and execution at `:354-365` receives the original context with no call-deadline propagation. A call admitted one nanosecond before expiry can run indefinitely, and context cancellation is classified as generic failure rather than S7 CANCELLED. Bind the grant to a canonical exact-call digest and have S7 supply/enforce the attempt context.

5. **[OK] Round-1 #4 is folded.** `internal/kernel/effectpath/effectpath.go:274-294,360-365` classifies an effectful executor error without a proving receipt as `OutcomeUnknown` and attaches `ErrEffectUnknown`; `internal/kernel/loop/loop.go:255-259` stops the turn. The new effectful-error RED observes the S7 UNKNOWN state.

6. **[UNFOLDED] Round-1 #5 was narrowed to three fields rather than full `ToolResult` validation.** `internal/kernel/effectpath/effectpath.go:367-373` checks only `ToolCallID`, `AttemptNo`, and `Status`. It does not enforce the canonical checks already present in `contracts.NewToolResult` (`internal/kernel/contracts/contracts.go:477-515`): ordered/nonzero timestamps, required typed error for FAILED, typed-error validity, output-block normalization, and receipt validity. A SUCCEEDED result with an invalid content/content_ref XOR, forged hash metadata, or invalid timestamps can pass the effect path and reach prompt assembly because `observeGuard` also validates only trust, lineage, and a few producer strings (`internal/kernel/loop/loop.go:99-125`). Add a canonical `ValidateFor(call)`/normalization seam and use it here.

7. **[UNFOLDED] Round-1 #6 fixes the pre-execution lie but not the crash/post-effect window.** The key adjudication is sound: AUTHORIZED→CANCELLED is a legal Annex P0.1 transition, matches the authority after `Cancel`, and honestly records that DENY/ASK ran no physical attempt (`internal/kernel/loop/loop.go:219-230`). But `attempt.started` is now appended only *after* synchronous `RunTool` returns (`:219-240`). A crash after grant consumption or an external commit but before return leaves the durable journal at AUTHORIZED, with no evidence that anything ran. If either post-effect append fails, the code merely puts “treat ... as UNKNOWN” in an error string; it neither transitions S7 to UNKNOWN nor durably appends `attempt.lost`, and S7 may already be terminal. Emit STARTED after successful consume and before dispatch through an authoritative callback/transactional seam; make post-dispatch persistence uncertainty durable and reconcilable.

8. **[OK] Round-1 #7 is folded.** Attempt envelopes now carry both `TurnID` and `ToolCallID` (`internal/kernel/loop/loop.go:79-92`), and `TestTwoToolCallsReplayPerAttempt` independently folds two terminal attempt streams (`internal/kernel/loop/loop_test.go:200-245`).

9. **[UNFOLDED] Round-1 #8 fixed instruction trust but not secret disclosure.** `internal/kernel/loop/loop.go:128-146` correctly downgrades raw tool errors to bounded `UNTRUSTED_EXTERNAL`, so prompt injection is fenced. It still copies raw `toolErr.Error()` with no known-reference redaction, marks sensitivity as the lowest literal value, and sends it to the next external provider at `:260-267`. A subprocess/provider error containing a credential-bearing URL or token is therefore still exfiltrated to the model. The round-1 finding explicitly covered both trust laundering and secrets; no rejection or deferral reason addresses the latter.

10. **[UNFOLDED] Round-1 #9 is fixed at the interface but not in the committed production artifact.** `internal/kernel/effectpath/effectpath.go:321-327` now refuses YOLO when `AuditSink.Record` fails, which is correct. The only real `journalAudit` implementation—with atomic identity, bounded context, and returned append error—exists in ignored `cmd/nexus/main.go:71-101`, not in HEAD (finding 1). The committed revision therefore contains no production audit sink or composition root proving the F2 invariant.

11. **[NEW-ERROR] The new plaintext-loopback exception accepts remote hostnames beginning with `127.`.** `internal/llm/provider/provider.go:76-85` treats any host string with prefix `127.` as loopback. Thus `http://127.attacker.example` passes the cleartext exception when allowlisted even though DNS can resolve it to a remote address, sending the bearer key without TLS. Parse the host (including port) as an IP and require `net.IP.IsLoopback`; do not infer address class from a hostname prefix. The original redirect re-check and non-loopback HTTPS requirement are otherwise folded, and the explicitly documented E11 IP-pinning/S6.3 ceiling is acceptable.

12. **[OK] Round-1 #11 is folded.** `internal/kernel/effectpath/effectpath.go:263-272` always preserves the causal error even when `OnError` returns nil, and the hostile middleware RED proves the refusal cannot become success.

13. **[UNFOLDED] Round-1 #12's full-spine proof is not revision-bound and still does not execute the PEP/effect path.** `TestFullSpineDeterministicTransport` does cross real `repl.Run` → UDS → daemon → loop → real planner → real provider transport/S7 → journal (`internal/app/daemon/daemon_test.go:303-383`). That is a substantial fold. But it constructs dependencies inside the test, while the actual production composition root is absent from HEAD, and its empty rule/tool maps plus final-only planner (`:338-346`) never call PEP, approval, tool grant, or `EffectPath.RunTool`. Therefore it does not cross every seam in the requested chain and cannot catch broken production wiring. Track the command entry point, then add a composition-root test; retain the declared tool-action ceiling until a real structured tool planner exists, but do not call the current test the entire PEP path.

14. **[OK] Round-1 #13 is folded at the committed module seams.** `PlannerFactory` binds a per-session delta sink (`internal/app/daemon/daemon.go:30-42,152-161`), `ChatPlanner` uses `Stream`, and the daemon emits delta frames before the final. Planner and REPL tests make those two halves causal. Production executability remains blocked by finding 1 rather than by the streaming implementation itself.

15. **[OK] Round-1 #14 is folded.** The ledger-literal `TestInProcessToolNeverSpawns` and `TestReadOnlyProcessStillSandboxed` now exist, and `TestUnknownEffectDiscriminatorReachesNoSink` uses closed discriminator fixtures with zero re-ask/sink assertions (`internal/llm/provider/provider_test.go:437-473`).

16. **[OK] Round-1 #15 is folded under the observed schedule.** `internal/foundation/procx/procx_linux.go:140-159` replaces the one-shot asynchronous SIGKILL check with bounded poll-and-rekill. The mandated full gate passed, and ten focused repetitions of both formerly failing process-tree tests passed. This is evidence against the prior flake on this host, not a universal scheduler proof.

17. **[OK] Kilo/Agy spot-check.** Kilo #1/#2 overlap findings 7 and 10; #3's E11 deferral is now explicitly owner/trigger-scoped; #5 producer spoofing is rejected; #7 trailing JSON is checked by EOF. Kilo #6's salvage-reporting intent is only partially successful because finding 3 leaves callback-error and failed-report paths unresolved. Kilo LOW #4 (`PayloadHash: "recomputed"`) was explicitly adjudicated as deferred API cleanup and does not weaken the journal, which recomputes it. Agy had no material round-1 findings to fold; its positive claims do not override the concrete counterexamples above.

Verification:

- `CGO_ENABLED=0 go vet ./... && CGO_ENABLED=0 go test -count=1 ./...` — PASS in the current working directory, including the ignored `cmd/nexus/main.go`.
- `CGO_ENABLED=0 go test -count=10 ./internal/foundation/procx -run '^(TestTerminateKillsWholeTree|TestTerminateReapsStragglerAfterPromptLeaderExit)$'` — PASS.
- Clean `git archive HEAD`: `cmd/nexus/main.go` absent; `CGO_ENABLED=0 go build ./cmd/nexus` — FAIL (`directory not found`). The archive-wide test also cannot be treated as a clean gate because existing buildcheck tests require `.git`, but that does not affect the direct missing-entry-point proof.

VERDICT: FAIL
