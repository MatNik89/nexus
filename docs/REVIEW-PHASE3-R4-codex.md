# Phase 3 verification round 4 — Codex

Target: `slice/p0-phase3` at `fcb0d6a4a3782b3737e856279eaa2d95e353310d`; reviewed only `91179fff22b22616674d8096eab4f5c983894162..fcb0d6a`.

Verification evidence:

- `CGO_ENABLED=0 go vet ./... && CGO_ENABLED=0 go test -count=1 ./...` — PASS.
- The three focused fold RED groups passed 20 times: memory migration/lineage, `QueryProjection` mutation, and deterministic tool prompt.
- Controlled clean-archive ablations proved the query and prompt REDs causal. A separate clean-archive production-tool probe reproduced finding 4 without modifying the repository.

1. **[OK] `SyncProjection.Version`/`Reset` and catch-up version handling fix the production upgrade path.** The journal stores `(name, applied_offset, version)` and resets when the checkpoint is absent or its version differs (`internal/kernel/journal/journal.go:316-350`; `internal/kernel/journal/projection.go:89-119`). Memory version 2 drops all owned derived objects and recreates the schema before canonical replay (`internal/memory/store.go:138-181`). The immediate predecessor's two-column checkpoint table is upgraded with `version DEFAULT 0`; that mismatch causes a full reset rather than patching the old projection. Missing-checkpoint state likewise resets intact tables before replay, avoiding the former duplicate-primary-key failure.

2. **[UNFOLDED] The checked-in “previous-revision database” RED does not actually construct the previous checkpoint schema.** `TestV1SchemaDatabaseRebuildsAtOpen` first creates the database with this HEAD, so `proj_sync_offsets.version` already exists; it then merely writes `version=1` and drops the memory `lineage` column (`internal/memory/facts_test.go:447-494`). Removing the production `ALTER TABLE proj_sync_offsets ADD COLUMN version ...` migration still leaves this RED green in a controlled ablation. Thus the implementation is correct by inspection, but the claimed older-revision migration detector cannot catch breakage of the exact schema transition it names. Build the fixture with the old two-column `proj_sync_offsets(name, applied_offset)` definition (or from a frozen `91179ff` fixture) and prove Open adds/version-checks it before rebuilding.

3. **[OK] The rebuild and ordinary append crash states are recoverable.** Before the reset checkpoint is rewritten, a crash leaves the missing/mismatched trigger in place, so the next Open resets again; after `(current version, offset 0)` is recorded, Open initializes and replays. Each replay step applies projection state and advances its checkpoint in one transaction (`internal/kernel/journal/journal.go:336-370`). Ordinary append likewise inserts the event, applies projection state, advances the checkpoint, and performs one final commit (`internal/kernel/journal/journal.go:458-508`; `internal/kernel/journal/projection.go:105-119`). Therefore no normal commit boundary can persist only one or two members of the `(event, projection, checkpoint)` triple.

4. **[UNFOLDED] A caller can still receive an AFTER_COMMIT receipt for an effect whose approved bytes were rewritten: put the known secret in `tags`.** `refuseSecretContent` asks the journal-owned redactor about only the JSON-encoded `content` string (`internal/memory/store.go:339-365`), while journal append redacts the entire fact payload, including tags (`internal/kernel/journal/journal.go:443-455`). A safe production-tool probe invoked `memory_remember` with content `safe fact` and tag `sk-TAG-SECRET`. The call succeeded and returned a receipt, but the stored tag was `[REDACTED:KEY]`. This bypasses the intended exact-intent guarantee even though the mismatched-redactor constructor bug is gone. Move the journal-owned rewrite check to the complete marshalled memory event payload (content, tags, IDs/options), and reject before append if any approved field would change; add this exact tag RED.

5. **[OK] The independent-redactor wiring defect itself is folded.** `NewStore` now accepts only the journal, and all content refusal decisions go through `Journal.RedactorRewrites`, which uses the journal's actual redactor (`internal/memory/store.go:333-365`; `internal/kernel/journal/journal.go:534-544`; `cmd/nexus/main.go:153-169`). No caller can supply a second redactor to the store and recreate the round-3 mismatch. Finding 4 is a field-coverage gap, not a second-redactor path.

6. **[OK] Supersession lineage is unioned through the production tool.** `memory_remember` passes the correction call ID to `SupersedeLineage`; inside the append transaction, Apply reads predecessor lineage and deterministically unions it with predecessor ID and correction lineage before inserting the leaf (`internal/memory/tools.go:59-80`; `internal/memory/store.go:251-315,431-445`). The production-tool RED verifies all three elements—`source-block`, `f-orig`, and `correction-call`—survive recall (`internal/memory/facts_test.go:497-532`). It passed 20 repetitions.

7. **[OK] The `QueryProjection` mutation RED is present and causal.** It exercises the formerly successful `SELECT; DELETE; SELECT` payload plus direct DELETE/INSERT and canonical-table reads, asserts rejection and unchanged projection rows, then verifies a legitimate read still works (`internal/kernel/journal/projection_test.go:369-399`). In a controlled ablation removing only the semicolon guard, this test failed on the multi-statement case with `probe not rejected: <nil>`.

8. **[OK] The deterministic-prompt RED is present and causal.** It captures the real provider-facing system prompt across eight fresh tool maps, requires one byte-distinct prompt, and asserts lexical tool order (`internal/llm/planner/planner_test.go:281-331`). Removing only `sort.Strings(ids)` made the RED fail with three byte-distinct prompt forms.

No additional material defect was found in the changed version/reset, lineage, query, or prompt lines. The fold remains incomplete because exact-intent receipt honesty still excludes tags, and the advertised prior-schema migration RED is not sensitive to removal of the actual checkpoint-table migration.

VERDICT: FAIL
